OAUTH_CLIENT_ID=sample-client-id
OAUTH_CLIENT_SECRET=sample-client-secret
OAUTH_ISSUER=https://sample-issuer.example.com
OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback

# Per-app URLs used by the portal cards. Set these to where each service is published on the host
# Example: http://localhost:8000
APP_DJANGO_URL=http://localhost:8000
APP_DOTNET_URL=http://localhost:5000
APP_PHP_URL=http://localhost:8080