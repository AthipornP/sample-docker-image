apiVersion: v1
kind: Service
metadata:
  name: dotnet8-web
  namespace: customer-portal
  labels:
    app: dotnet8-web
spec:
  type: ClusterIP
  selector:
    app: dotnet8-web
  ports:
    - name: http
      port: 80
      targetPort: 80
