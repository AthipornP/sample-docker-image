apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: grafana
  labels:
    app: prometheus
spec:
  type: NodePort
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
      name: web
      nodePort: 32090
  selector:
    app: prometheus
