apiVersion: v1
kind: Service
metadata:
  name: postgres-exporter
  namespace: grafana
  labels:
    app: postgres-exporter
spec:
  type: ClusterIP
  ports:
    - port: 9187
      targetPort: 9187
      protocol: TCP
      name: metrics
  selector:
    app: postgres-exporter
