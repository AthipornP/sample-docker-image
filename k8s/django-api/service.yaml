apiVersion: v1
kind: Service
metadata:
  name: django-api
  namespace: customer-portal
  labels:
    app: django-api
spec:
  type: NodePort
  selector:
    app: django-api
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      nodePort: 30003
